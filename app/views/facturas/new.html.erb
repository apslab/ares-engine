<% content_for(:breadcrumbs) do %>
    <%= breadcrumbs do |breadcrumb|
      breadcrumb.item current_company.name.titlecase, root_path
      breadcrumb.item Cliente.model_name.human, clientes_path
      breadcrumb.item @factura.cliente.razonsocial, cliente_path(@factura.cliente)
      breadcrumb.item 'Nueva Factura', cliente_path(@factura.cliente), :active => true
    end %>
<%end%>

<% content_for(:controls) do %>
<%= controls do |c|
         c.item "Volver", cliente_path(@factura.cliente), :icon => "application_go"
end %>
<%end%>

<%= render 'form' %>