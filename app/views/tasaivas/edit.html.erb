<h1>Editando Tasa de IVA</h1>

<% content_for(:controls) do %>
<%= controls do |c|
         c.item "Ver", tasaiva_path(@tasaiva), :icon => "application_go"
end %>
<%end%>

<% content_for(:breadcrumbs) do %>
<%= breadcrumbs do |b|
	b.item current_company.name.titlecase, root_path
    b.item "Tasa de IVA", tasaivas_path
    b.item @tasaiva.detalle, root_path, :active => true
end %>
<%end%>

<%= render 'form' %>